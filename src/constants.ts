export const DEFAULT_LIMIT = 5;

export const THUMBNAIL_FALLBACK = "/placeholder.svg";

// Change to custom env if deploy outside Vercel
export const APP_URL = process.env.NEXT_PUBLIC_APP_URL || "http://localhost:3000";